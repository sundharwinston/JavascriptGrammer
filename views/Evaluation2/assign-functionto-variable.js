var calculate = function(first,last) { 						// Assign a function to variable
	return first + last;									// return a value from calling place
}
let data = calculate(100,10);								// function invocation
console.log(data);



// Arrow function
var sum = (first,last) => {
	return first + number.a;
}
console.log(sum(100,{number:1}));


